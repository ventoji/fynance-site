import React from 'react';
import logo from "../../static/images/header.jpg" 

const ImageTest = () => (
    <div style={{
        background: "url(" +` ${logo}`+")"
    }}>
    
    </div>
    
    );

export default ImageTest;